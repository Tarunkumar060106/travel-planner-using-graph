#To run the application, run the following commands

#Ensure g++ is installed on linux/bash/cmd terminal 

#Creating objects
g++ -c travelplanner.c 

#Client
g++ -o client client.c

#Running client
./client 
